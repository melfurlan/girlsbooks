<%= link_to 'Novo usuario', new_user_path %>
<hr>
<% @users.each do |user| %>
	Usuarios:
	<br>
	Nome:
	<%= user.name %>
	<br>
	Data de nascimento:
	<%= user.birth %>
	<br>
	Email:
	<%= user.email %>
	<br>
	<%= link_to 'editar', edit_user_path(user.id) %>
	<%= link_to 'apagar', user_path(user.id), method: :delete %>

	<hr>
<% end %>